NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
JWT_SECRET=<GENERATE: openssl rand -base64 32>
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=<GENERATE: openssl rand -base64 32>
REFRESH_TOKEN_EXPIRES_IN=7d
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
# Rate Limit (기본값: 60초당 300회)
THROTTLE_TTL=60
THROTTLE_LIMIT=300

# Redis (OAuth 캐시, 멀티 인스턴스 지원)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=<GENERATE: openssl rand -base64 24>
# REDIS_DB=0

# Security
IP_HASH_SALT=<GENERATE: openssl rand -hex 16>

# File Encryption (인증 서류 암호화 - 프로덕션 필수!)
# 교사 신분 증명 서류를 AES-256-GCM으로 암호화합니다
# 프로덕션에서 반드시 설정 필요 (최소 32자 이상 랜덤 문자열)
FILE_ENCRYPTION_KEY=<GENERATE: openssl rand -base64 32>

# ============================================
# Mail (프로덕션에서 필수!)
# ============================================
# 이메일 인증이 회원가입 필수 단계이므로
# 프로덕션 배포 시 반드시 SMTP 설정 필요
#
# 개발 환경: 설정 없이도 콘솔에 인증 코드 출력
# 프로덕션: 아래 설정 필수
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-16-chars
MAIL_FROM=noreply@teacher-shelter.com
MAIL_FROM_NAME=교사쉼터
FRONTEND_URL=http://localhost:3001

# ============================================
# OAuth2 (Phase 2)
# ============================================
# Google OAuth
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Kakao OAuth
# https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_CALLBACK_URL=http://localhost:3000/api/auth/kakao/callback

# Naver OAuth
# https://developers.naver.com/apps
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
NAVER_CALLBACK_URL=http://localhost:3000/api/auth/naver/callback
