import type { LoggerService } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../database/prisma.service';
import { RedisService } from '../redis/redis.service';
import { MailService } from '../mail/mail.service';
import { LoginDto } from './dto/login.dto';
import { RegisterDto } from './dto/register.dto';
import { TokenDto } from './dto/token.dto';
import { OAuthUserDto } from './dto/oauth-user.dto';
export declare class AuthService {
    private prisma;
    private jwtService;
    private configService;
    private mailService;
    private redisService;
    private readonly logger;
    private readonly SALT_ROUNDS;
    private readonly REFRESH_TOKEN_EXPIRY_DAYS;
    private readonly PASSWORD_RESET_EXPIRY_HOURS;
    private readonly EMAIL_VERIFICATION_EXPIRY_MINUTES;
    private readonly OAUTH_CODE_EXPIRY_SECONDS;
    private readonly OAUTH_CODE_PREFIX;
    private readonly MAX_OAUTH_CODES;
    private oauthCodeFallback;
    constructor(prisma: PrismaService, jwtService: JwtService, configService: ConfigService, mailService: MailService, redisService: RedisService, logger: LoggerService);
    register(registerDto: RegisterDto): Promise<TokenDto>;
    login(loginDto: LoginDto): Promise<TokenDto>;
    oauthLogin(oauthUser: OAuthUserDto): Promise<TokenDto>;
    private findOrCreateOAuthUser;
    refresh(userId: string, oldRefreshToken: string): Promise<TokenDto>;
    logout(refreshToken: string): Promise<void>;
    blacklistFromAccessToken(authHeader: string | undefined): Promise<void>;
    private calculateAccessTokenExpiry;
    private generateVerificationCode;
    sendVerificationEmail(userId: string, email: string, nickname: string): Promise<void>;
    verifyEmail(userId: string, code: string): Promise<{
        message: string;
    }>;
    resendVerificationEmail(userId: string): Promise<void>;
    forgotPassword(email: string): Promise<void>;
    resetPassword(token: string, newPassword: string): Promise<void>;
    private validateUser;
    private getBanMessage;
    private generateAndSaveTokens;
    private hashToken;
    private hashPassword;
    private comparePassword;
    generateOAuthCode(accessToken: string): Promise<string>;
    exchangeOAuthCode(code: string): Promise<string>;
    private cleanupExpiredFallbackCodes;
}
